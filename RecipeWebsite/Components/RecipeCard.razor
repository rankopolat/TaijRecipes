@using RecipeWebsite.Models
@using RecipeWebsite.Helpers
@inject NavigationManager Navigation

<MudCard Class="recipe-card" Elevation="2" Style="cursor: pointer; height: 100%;"
         @onclick="@(() => Navigation.NavigateTo($"recipe/{Recipe.Id}"))">
    @if (!string.IsNullOrEmpty(Recipe.ImageUrl))
    {
        <MudCardMedia Image="@Recipe.ImageUrl" Height="200" />
    }
    else
    {
        <MudCardMedia Image="@PlaceholderImages.GetForCategory(Recipe.CategoryName)"
                      Height="200" />
    }
    <MudCardContent>
        <MudText Typo="Typo.h6" Class="mb-1">@Recipe.Title</MudText>
        @if (!string.IsNullOrEmpty(Recipe.CategoryName))
        {
            <MudChip T="string" Color="Color.Primary" Size="Size.Small" Class="mb-1">@Recipe.CategoryName</MudChip>
        }
        @if (!string.IsNullOrEmpty(Recipe.Description))
        {
            <MudText Typo="Typo.body2" Color="Color.Tertiary" Class="mt-1"
                     Style="overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;">
                @Recipe.Description
            </MudText>
        }
        <div class="d-flex gap-2 mt-2">
            @if (Recipe.PrepTimeMinutes.HasValue || Recipe.CookTimeMinutes.HasValue)
            {
                <MudText Typo="Typo.caption" Color="Color.Tertiary">
                    <MudIcon Icon="@Icons.Material.Filled.Timer" Size="Size.Small" />
                    @((Recipe.PrepTimeMinutes ?? 0) + (Recipe.CookTimeMinutes ?? 0)) min
                </MudText>
            }
            @if (Recipe.Servings.HasValue)
            {
                <MudText Typo="Typo.caption" Color="Color.Tertiary">
                    <MudIcon Icon="@Icons.Material.Filled.People" Size="Size.Small" />
                    @Recipe.Servings
                </MudText>
            }
        </div>
    </MudCardContent>
</MudCard>

@code {
    [Parameter, EditorRequired]
    public Recipe Recipe { get; set; } = default!;
}